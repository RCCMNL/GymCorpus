<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Health Connect Permissions -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_DISTANCE" />
    <uses-permission android:name="android.permission.health.READ_TOTAL_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    
    <!--
        Location Permissions

        Solo posizione in primo piano: il tracciamento cardio avviene mentre
        l'utente e' sulla schermata, e Geolocator.requestPermission() chiede
        esclusivamente il permesso "while in use".

        ACCESS_BACKGROUND_LOCATION, FOREGROUND_SERVICE e
        FOREGROUND_SERVICE_LOCATION erano dichiarati ma inutilizzati: nessun
        <service> esiste nel progetto e il permesso di background non veniva
        mai richiesto a runtime, quindi non funzionava comunque. Sono permessi
        sensibili che Google Play esamina con un modulo di giustificazione
        dedicato, e dichiararli senza usarli porta al rifiuto della
        pubblicazione.

        Per tracciare davvero a schermo spento servirebbe un foreground
        service con tipo "location", piu' la richiesta esplicita del permesso
        di background come passaggio separato.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- App Permissions -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
        allowBackup e' disabilitato di proposito: il database locale contiene
        dati sanitari ed e' cifrato con una chiave custodita nel Keystore, che
        non finisce nel backup. Lasciando il backup attivo si esporterebbe un
        file inutilizzabile, e con esso i dati delle versioni precedenti
        ancora in chiaro. dataExtractionRules copre lo stesso caso su
        Android 12 e successivi, dove fullBackupContent non viene piu' letto.
    -->
    <application
        android:label="GymCorpus"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
            
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />

            <!-- RICHIESTO DA HEALTH CONNECT -->
            <meta-data 
                android:name="androidx.health.permissions.privacy_policy_url" 
                android:value="https://gymcorpus.app/privacy" />

            <!-- INDICA CHE QUESTA ACTIVITY GESTISCE I PERMESSI SALUTE -->
            <meta-data 
                android:name="health_permissions_rationale_activity" 
                android:value=".MainActivity" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- CONTRATTO OBBLIGATORIO PER HEALTH CONNECT -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mailto" />
        </intent>
        <package android:name="com.google.android.apps.healthdata" />
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
    </queries>
</manifest>
